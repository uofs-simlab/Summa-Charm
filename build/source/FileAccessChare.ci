module FileAccessChare {
  include "file_access_chare.hpp";

  chare FileAccessChare {
    entry FileAccessChare(NumGRUInfo num_gru_info, FileAccessActorSettings fa_settings);
    entry [threaded] void initFileAccessChare(int file_gru, int num_hru, CkCallback cb);
    entry void accessForcing(int iFile);
    entry void restartFailures();
    entry void finalize();
    entry void error(int err_code, std::string err_msg);
  };
};