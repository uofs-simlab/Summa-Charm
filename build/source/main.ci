mainmodule mainChare {
  extern module SummaChare;
  extern module JobChare;
  extern module GruChare;
  extern module FileAccessChare;

  
  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done();
  };
};
