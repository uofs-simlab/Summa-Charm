module JobChare {
  include "batch.hpp";
  
  chare JobChare {
    entry JobChare(Batch batch, CkChareID summa_chare_proxy, int file_gru);
    entry void processGRU(int gru_id);
    entry void finalize();
    entry void fileAccessReady(int num_steps);
    entry void handleError(int err_code, std::string err_msg);
  };
}